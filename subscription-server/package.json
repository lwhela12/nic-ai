{
  "name": "claude-pi-subscription-server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npx tsx --watch index.ts",
    "db:migrate": "npx tsx db/migrate.ts",
    "db:seed": "npx tsx db/seed.ts"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0",
    "hono": "^4.4.0",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
